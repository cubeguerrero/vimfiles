# Neovim Config

## Setup
1. Create a directory under `~/.config/` by running `mkdir ~/.config/nvim`
2. Clone this repository `git clone https://github.com/cubeguerrero/vimfiles ~/.config/nvim`
3. Open `neovim` and run `:PackerInstall`
